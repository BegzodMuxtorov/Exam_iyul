import 'package:flutter/material.dart';

class PolitcsTab extends StatelessWidget {
  const PolitcsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('3'),
    );
  }
}